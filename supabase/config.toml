project_id = "xlxouevfbkicvepxuiyk"

[functions.mailerlite-subscribe]
verify_jwt = false

[functions.send-thank-you-email]
verify_jwt = false

# Public survey submission. Validates and spam-checks before writing.
[functions.restaurant-submit]
verify_jwt = false

# Admin-only: called while publishing a listing, so it keeps JWT verification.
[functions.geocode-address]
verify_jwt = true
# Admin-only: emails a restaurant on an admin's behalf. Keeps JWT verification
# and re-checks the admin role inside the function.
[functions.restaurant-notify]
verify_jwt = true

# Admin-only: generates the improvement report PDF and emails it. Keeps JWT
# verification and re-checks the admin role inside the function, the same way
# restaurant-notify does.
[functions.restaurant-report]
verify_jwt = true

# Public: records the optional resources a restaurant asks for after
# submitting. Writes three booleans and nothing else.
[functions.restaurant-interests]
verify_jwt = false

# Public: "help bring AllergyVoices to your city" form. Records the request,
# emails the team, and subscribes the person if they asked.
[functions.city-request]
verify_jwt = false
